<style>
    {% if base.imageNameFon is not null %}
    .background{
        display: block;
        position: fixed;
        background: url("{{ vich_uploader_asset(base, 'imageFileFon') }}") center center no-repeat;
        background-size: cover;
        width: 100%;
        height: 100vh;
        z-index: 0;
    }
    {% else %}
    {% if base.fonRGB is not null %}
    .background{
        background-color: rgb{{ base.fonRGB }};
    }
    {% endif %}
    {% endif %}
</style>